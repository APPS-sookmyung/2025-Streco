const SectionTitle = ({ text }) => {
  return (
    <h4 className="mt-[10px] ml-[15px] mb-0 pt-[15px] pb-[5px] text-[16px] font-medium text-white">
      {text}
    </h4>
  );
};

export default SectionTitle;
