const SectionTitle = ({ text }) => {
  return (
    <h4 className="mt-[10px] ml-[15px] mb-0 pt-[15px] pb-[15px] text-[18px] font-semibold">
      {text}
    </h4>
  );
};

export default SectionTitle;
